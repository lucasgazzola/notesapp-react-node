// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://fl0user:A6XChIKWZOR0@ep-damp-cell-a5bhf6ny.us-east-2.aws.neon.fl0.io:5432/notes?sslmode=require"
}

model Note {
  id         Int                 @id @default(autoincrement())
  title      String
  content    String
  isArchived Boolean             @default(false)
  updatedAt  DateTime            @default(now()) @updatedAt
  categories CategoriesOnNotes[]
}

model Category {
  name  String              @id @unique
  notes CategoriesOnNotes[]
}

model CategoriesOnNotes {
  note         Note     @relation(fields: [noteId], references: [id])
  noteId       Int
  category     Category @relation(fields: [categoryName], references: [name])
  categoryName String


  @@id([noteId, categoryName])
}
